package com.array;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashSet;

public class St9_Union_Of_Two_Sorted_Array {
/*
    public static ArrayList <Integer> UnionOfSortedArray (int [] arr1, int [] arr2){
        HashMap <Integer,Integer> hashSet = new HashMap<>();
        ArrayList<Integer> Union = new ArrayList<>();

        for (int i = 0; i < arr1.length; i++) {
            hashSet.put(arr1[i] , hashSet.getOrDefault(arr1[i],0) + 1);

        }

        for (int i = 0; i < arr2.length; i++) {
            hashSet.put(arr2[i] , hashSet.getOrDefault(arr2[i],0) + 1);
        }

        for (int it : hashSet.keySet()){
            Union.add(it);
        }
        return  Union;

    }

    public static void main(String[] args) {
        int [] arr1 = {1, 1, 2, 3, 4, 5};
        int [] arr2 = {2, 3, 4, 4, 5, 6};
        System.out.println(Arrays.toString(arr1));
        System.out.println(Arrays.toString(arr2));

        ArrayList<Integer> Union = UnionOfSortedArray(arr1, arr2);
        System.out.println("Union of arr1 and arr2 is ");

        for (int value : Union)
            System.out.println(value + " ");
    }

 */
}
